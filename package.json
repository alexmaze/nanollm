{
  "name": "nanollm",
  "version": "0.0.21",
  "description": "Hono server that proxies llm request",
  "main": "./dist/server.js",
  "bin": {
    "nanollm": "dist/cli.js"
  },
  "files": [
    "dist",
    "frontend/dist",
    "README.md"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:ui\"",
    "dev:backend": "tsx --watch server.ts",
    "dev:ui": "npm run dev --prefix frontend",
    "build": "npm run build:backend && npm run build:ui",
    "build:backend": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.dist.json",
    "build:ui": "npm run build --prefix frontend",
    "prepack": "npm run build:backend",
    "start": "tsx server.ts",
    "start:railway": "bash scripts/start-railway.sh",
    "typecheck": "npm run typecheck:backend && npm run typecheck --prefix frontend",
    "typecheck:backend": "tsc --noEmit",
    "test": "tsc -p tsconfig.build.json && node .test-dist/tests/run.js",
    "converter:test": "tsc -p tsconfig.build.json && node .test-dist/src/converters/test.js"
  },
  "keywords": [
    "llm",
    "proxy",
    "hono",
    "litellm"
  ],
  "author": "sunwu51",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "@hono/node-server": "^1.19.13",
    "dotenv": "^17.3.1",
    "hono": "^4.12.12",
    "https-proxy-agent": "^8.0.0",
    "openai": "^6.32.0",
    "undici": "^7.24.5",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "concurrently": "^9.2.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
